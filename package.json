{
  "name": "lake",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "bun run --filter @lake/site dev",
    "build": "bun run --filter @lake/site build",
    "start": "bun run --filter @lake/site start"
  }
}
